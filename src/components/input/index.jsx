import { useState } from "react";
const Input = (props) => {

    return (
        <>
            <input type={props.type} className="w-full rounded py-1 pl-1 text-blue-900 " name="" id="" />
        </>
    );
}

export default Input;