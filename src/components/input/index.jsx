const Input = ({type}) => {
    
    return (
        <>
            <input type={type} className={`w-full rounded py-1 text-md text-white`} name="" id="" />
        </>
    );
}

export default Input;